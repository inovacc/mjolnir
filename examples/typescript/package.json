{
  "name": "mjolnir-example",
  "version": "0.1.0",
  "description": "Mjolnir Example API - TypeScript",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^7.0.2"
  }
}
