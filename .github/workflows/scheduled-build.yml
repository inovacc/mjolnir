name: Scheduled Build

on:
  schedule:
    # Run every two weeks on Monday at 00:00 UTC
    - cron: '0 0 1,15 * *'
  workflow_dispatch: # Allow manual trigger

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  GO_VERSION: "1.27.0"

jobs:
  build-and-push-debian:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      security-events: write
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Debian image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.debian
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          # H-05: force a from-scratch rebuild so the scheduled "security
          # rebuild" actually pulls fresh base/apt layers instead of reusing
          # cached ones. cache-to still refreshes the shared cache for PR builds.
          no-cache: true
          cache-to: type=gha,mode=max,scope=debian

      - name: Verify Debian image
        run: docker run --rm --read-only --tmpfs /tmp --security-opt=no-new-privileges ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest

      - name: Scan Debian image
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          format: 'sarif'
          output: 'trivy-debian.sarif'
          severity: 'CRITICAL,HIGH'
        continue-on-error: true

      - name: Upload Debian scan results
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: 'trivy-debian.sarif'
          category: scheduled-debian
        continue-on-error: true

  build-and-push-alpine:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      security-events: write
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Alpine image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.alpine
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest-alpine
          # H-05: force a from-scratch rebuild so the scheduled "security
          # rebuild" actually pulls fresh base/apk layers instead of reusing
          # cached ones. cache-to still refreshes the shared cache for PR builds.
          no-cache: true
          cache-to: type=gha,mode=max,scope=alpine

      - name: Verify Alpine image
        run: docker run --rm --read-only --tmpfs /tmp --security-opt=no-new-privileges ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest-alpine

      - name: Scan Alpine image
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest-alpine
          format: 'sarif'
          output: 'trivy-alpine.sarif'
          severity: 'CRITICAL,HIGH'
        continue-on-error: true

      - name: Upload Alpine scan results
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: 'trivy-alpine.sarif'
          category: scheduled-alpine
        continue-on-error: true
